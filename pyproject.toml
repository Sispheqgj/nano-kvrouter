[project]
name = "nano-kvrouter"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Guanjia Qiao", email = "153741600+Sispheqgj@users.noreply.github.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0",
    "rich>=13.0",
    "pyyaml>=6.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[build-system]
requires = ["uv_build>=0.11.14,<0.12.0"]
build-backend = "uv_build"
